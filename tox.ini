[tox]
envlist = py36,py37,py38

skipsdist = True

[testenv]
commands =
    {toxinidir}/scripts/ci/install
    {toxinidir}/scripts/ci/run-tests

[testenv:lint]
deps =
    -r requirements-dev.txt
commands =
    black --check botocore tests
    flake8 botocore tests
